{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Service Schema",
  "description": "Schema for pawnshop services - structured for LLM retrieval",
  "type": "object",
  "required": ["service_id", "name", "category"],
  "properties": {
    "service_id": {
      "type": "string",
      "description": "Unique identifier for the service"
    },
    "name": {
      "type": "string",
      "description": "Service name"
    },
    "category": {
      "type": "string",
      "description": "Service category (e.g., loans, buying, selling)",
      "enum": ["loans", "buying", "selling", "appraisals", "other"]
    },
    "description": {
      "type": "string",
      "description": "Factual description of the service (verified only)"
    },
    "item_categories": {
      "type": "array",
      "description": "Categories of items this service applies to",
      "items": {
        "type": "string"
      }
    },
    "available_at_locations": {
      "type": "array",
      "description": "Location IDs where this service is available",
      "items": {
        "type": "string"
      }
    },
    "compliance_notes": {
      "type": "string",
      "description": "Any compliance or regulatory notes (NY Article 5)"
    }
  }
}
